<Window x:Class="TimeQuestLogDesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeQuestLogDesktopApp" xmlns:views="clr-namespace:TimeQuestLogDesktopApp.Views" d:DataContext="{d:DesignInstance Type=views:MainViewModel}"
        xmlns:vms="clr-namespace:TimeQuestLogDesktopApp.ViewModels"
        mc:Ignorable="d"
        Title="TimeQuest Log" Height="720" Width="1200">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:LoginViewModel}">
                <views:LoginView />
            </DataTemplate>
        </Grid.Resources>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
