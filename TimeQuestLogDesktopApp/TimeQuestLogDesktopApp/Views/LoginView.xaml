<UserControl x:Class="TimeQuestLogDesktopApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeQuestLogDesktopApp.Views" xmlns:viewmodels="clr-namespace:TimeQuestLogDesktopApp.ViewModels" xmlns:components="clr-namespace:TimeQuestLogDesktopApp.Components" d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#121212">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
            <Label
            Grid.Column="1" 
            Grid.Row="0" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
            <TextBlock TextWrapping="WrapWithOverflow" FontSize="20" Foreground="White">
                Welcome to QuestTime Log!
            </TextBlock>
        </Label>
        <StackPanel Grid.Column="1" Grid.Row="1" Background="#252525">
            <Label
                Grid.Column="1" 
                Grid.Row="1" 
                Content="Username" 
                Foreground="White"
                HorizontalAlignment="Center"  
                VerticalAlignment="Center"/>
            <TextBox 
                Height="25"
                BorderBrush="#b23a48"
                Grid.Column="1" 
                Background="#252525"
                HorizontalAlignment="Center" 
                Grid.Row="1" 
                TextWrapping="Wrap" 
                Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                VerticalAlignment="Center"
                Foreground="White"
                VerticalContentAlignment="Center"
                Width="200"/>

        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="2" Background="#252525">
            <Label
                Grid.Column="1" 
                Grid.Row="1" 
                Content="Password" 
                HorizontalAlignment="Center"  
                Foreground="White"
                VerticalAlignment="Center"/>
            <components:BindablePasswordBox
                Password="{Binding Password, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                />

        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="3" Background="#252525" Height="50">
            <Button
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="Login"
                Command="{Binding LoginCommand}"
                Foreground="White"
                Background="#b23a48"
                Height="30"
                Width="100"/>

        </StackPanel>

    </Grid>
</UserControl>
